name = "vibes-vibe-standards-org"
main = "worker.js"
compatibility_date = "2026-02-24"

[assets]
directory = "../../www/vibe-standards.org"
binding = "ASSETS"

# DNS/deployment directive:
# add route + zone mapping after Cloudflare DNS is finalized.
# Example:
# routes = [{ pattern = "vibe-standards.org/*", zone_name = "vibe-standards.org" }]
